import React from 'react'
import styles from './Calendar.module.scss'

const Calendar = () => {
  return (
    <div className={'container' + " " + styles.calendar_container}>
      Calendar
    </div>
  )
}

export default Calendar